import LoaderLottie from '../../assets/animations/loader.json';

const ANIMATION = {
  LOADER: 'loader' as 'loader',
};

const ANIMATION_DATA = {
  [ANIMATION.LOADER]: LoaderLottie,
};

export { ANIMATION, ANIMATION_DATA };

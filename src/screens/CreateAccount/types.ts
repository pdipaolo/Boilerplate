type UserType = {
  email: string | null,
  password: string | null,
  confirmPassword: string | null,
  name: string | null,
  lastname: string | null,
  city: string | null,
  address: string | null,
};

export { type UserType};

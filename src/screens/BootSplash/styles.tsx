import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
  },
  loaderContainer: {
    flex: 1,
    marginTop: 140,
  },
});

export { styles };
